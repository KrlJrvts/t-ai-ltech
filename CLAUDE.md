# Claude Code Notes

Use `AGENTS.md` as the main instruction set for this repository.

## Claude-specific expectations

- Prefer the course-specific folders when a task maps to a language or course topic.
- Keep recommendations aligned with TalTech `IADB17`.
- Bias toward maintainable, explainable code over novelty.
- Keep demos, reports, and grading requirements in mind.

## Repository context

This repo contains the browser runtime under `browse/`, plus generic QA and review helpers. The TalTech-specific workflow lives in the top-level course-specific folders.

## Commands

```bash
bun install
bun test
bun run dev <cmd>
bun run build
./setup
```
